# 02 — Early Geometry / "Alien" Invention Engines

First automatic-invention attempts: geometry-to-Zig compilation (engine_genesis),
novelty-pressure probes, alien-breakthrough and phase-lattice inventors.
SUPERSEDED by DomainSpec (thread 04). See `docs/` and ../RESEARCH_INDEX.md.

```sh
zig build      # 13 executables
```
Depends on `../core`.
