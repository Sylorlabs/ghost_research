# 01 — Reservoir Engines (VSA era)

Comparative benchmarks of the Flare/Flame/Flux/Fractal/Frost engines against the
VSA baseline — the iterative reservoir-computer experiments. SUPERSEDED as a
synthesis approach; the reservoir runtime itself (flame/void/flux) lives in
`../core`. See `docs/` (and `docs/DISCLAIMER.md`) and ../RESEARCH_INDEX.md.

```sh
zig build      # 6 executables
```
Depends on `../core`.
