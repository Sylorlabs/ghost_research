{
  "summary": "Develop genuinely-beyond-known-methods directions to break the per-token I/O floor for V4 on 16GB: context-locality, local function surrogates, coarse-to-fine super-experts, output memoization. Mine sibling sparse_poly/function_hardness for concrete methods, adversarially test each against the information floor, return runnable experiments.",
  "agentCount": 11,
  "logs": [
    "Phase A complete: methods mined, floor re-derived. Developing floor-breaking directions."
  ],
  "result": {
    "synthesis": "I now have everything verified from disk. The decisive new finding the arithmetic forces: even with phi driven to zero (fetch fully eliminated), the **compute wall is 2.78 tps** — 20 tps is below it by ~7x. This reframes the entire question. I have enough to deliver the honest synthesis.\n\n---\n\n# 20 TPS: HONEST RE-RANKING AND THE REAL FLOOR\n\nI verified every load-bearing number against data on disk before writing this. The adversarial verdicts hold, and one verification reframes the whole question.\n\n## 1. THE HONEST FLOOR: physical law vs. breakable framing\n\n**Two floors exist, and they were conflated.** The 0.23 tps \"floor\" is a *fetch* floor, and it is the soft one — pure framing, breakable. Underneath it is a *compute* floor that is hard.\n\n**The fetch floor (breakable framing, confirmed mislabeled by ~2x).** I recomputed phi(C) directly from the U3 cap sweep on real sequential score routing (e11_positions/u3_screen, 58 layers, 348 demanded fetches/token):\n\n| config | phi | t_fetch (P3) | tps (B=1) |\n|---|---|---|---|\n| phi=1.0 (the e10b \"floor\") | 1.000 | 8.25s | 0.116 |\n| cap8 no-policy (16GB-feasible) | 0.651 | 5.37s | 0.175 |\n| cap128 +eps policy | 0.189 | 1.56s | **0.520** |\n| cap256 +eps policy | 0.186 | 1.53s | 0.529 |\n\nThe 0.23 number assumed phi=1.0. Measured phi at a feasible cache is 0.19–0.65. So the fetch floor is real but mislabeled: the honest single-stream ceiling is **~0.5 tps, not 0.23** — *if* you can afford cap-128 RAM, which the 16GB box cannot (cap-128 P3 ≈ 95GB resident). On 16GB you are stuck near cap-8 → **~0.18–0.27 tps**. So far the verdicts are right: every direction that fights phi alone relocates cost and lands in a wash near the real cap-8 floor.\n\n**The compute floor (physical law, the actual wall).** Here is the verification that reframes everything: drive phi to **zero** — assume every cold fetch is free — and tps = 1/t_compute = **2.78 tps**. The XNOR kernel + 12 threads spend 0.36s/token regardless of disk. **20 tps requires t_total < 0.05s/token; t_compute alone is 0.36s. 20 tps is below the compute floor by ~7x on this silicon, even with infinite bandwidth and infinite RAM.**\n\n**Verdict on the headline question: 20 tps interactive is NOT reachable by any of these four directions, nor by any retraining-free method, on this 16GB box — and the binding reason is NOT the information floor, it is the CPU compute floor.** The information/fetch floor is breakable framing worth ~4x (0.12 → ~0.5). The 2.78 tps compute wall is physical law on a Ryzen 5600X running an XNOR MoE. The cold-knowledge fraction debate (phi 0.08 vs 1.0) is real and matters for cost, but it is a fight over the wrong floor — even winning it completely leaves you at 2.78 tps.\n\n## 2. THE MOST PROMISING FLOOR-BREAKING BET\n\nAll four named directions were correctly killed as standalone floor-breakers — I reproduced the kills:\n- **SWS-TES**: entropy gate is broken on disk (I measured Pearson(quant_nll, damage) = **+0.145**, not the claimed +0.474; top-10 damage ∩ top-10 quant_nll = **2/10**; high-entropy 8% carries **4.3%** of damage). ext4 free space is **45GB**, not 61/layer×58 = 95GB. KILL stands. Salvage: plain ungated U3 + the disk-tier as infra.\n- **LSC-Gate, C2F-superexperts, GHOST**: all relocate cost (bimodal input cloud, orthogonal-basis cancellation at norm-ratio 0.259, held-out surrogate cosine 0.50 vs 0.90 bar). KILLs stand.\n\n**The one bet that genuinely attacks a floor variable nobody measured is BATCH (B) — the speculative/MTP routed-expert UNION — stacked on cap-128 + ungated U3 substitution.** It is the only lever that converts per-token random-read *latency* into once-per-step *bandwidth*. Every cold expert read serves B sequences, so effective bytes/token divides by the union-amortization factor. My upper-bound arithmetic:\n\n| B | phi_eff (upper bound) | aggregate tps |\n|---|---|---|\n| 1 | 0.189 | 0.52 |\n| 8 | 0.024 | 1.80 |\n| 32 | 0.006 | **2.45** |\n\n**Honest upside: ~2.5 tps aggregate, asymptoting into the 2.78 tps compute wall.** Batch is the only direction that reaches the compute floor instead of the fetch floor — it is a genuine ~5–13x over the real cap-8 single-stream floor, and ~10x over the labeled 0.23. But it saturates at the compute wall and **does not approach 20 tps.** It is also not interactive in the single-stream sense — it buys aggregate throughput across B concurrent sequences, not latency for one.\n\nThe decisive *unknown* that makes-or-breaks B: does the routed-expert union across B sequences saturate toward 384 sublinearly (graduate → the table above) or grow as B×6 (die → no amortization)? **This is unmeasured and the trace to measure it does not exist on disk.**\n\n## 3. THE DECISIVE CHEAP EXPERIMENT\n\n**Generate the route-dump trace, then replay the batch-union curve offline.** I confirmed `ppl_stack_route` already has the `ROUTE_DUMP` env-var flag wired (src/ppl_stack.zig:691–696, 1070–1083) but **the trace `route_off8000.bin` does not exist** — this experiment is genuinely unrun.\n\n**Step 1 (one ~3000s run, no new hardware):**\n```\nROUTE_DUMP=route_off8000.bin ./ppl_stack_route  (over T>=256 coherent window)\n```\nEmits per-layer, per-token top-6 expert IDs.\n\n**Step 2 (offline ~20-line replay, zero model rerun):** Take B contiguous token positions as a pseudo-batch (or B offset windows). For each score layer, compute the **union** of routed expert IDs across the B sequences vs. the **sum** of per-sequence counts. The number that decides:\n\n> **union_ratio(B) = |union of routed experts across B seqs| / (B × 6), per layer, averaged.**\n\n- **GO**: union_ratio falls toward ~0.3–0.5 by B=8 and keeps dropping (sublinear union → experts are shared across sequences → amortization works → ~2.5 tps aggregate is real). Equivalently, marginal new-experts-per-added-sequence decays.\n- **STOP**: union_ratio stays ≥0.85 (each sequence routes to mostly-disjoint experts → union grows ~B×6 → batch buys nothing, fetch cost scales with B → dead, same as 0.52 tps × nothing).\n\nThis is the project's own E0 question, never run. Total cost: one overnight `ppl_stack_route` run + a numpy script over the trace and the existing `e4_routing.csv`. The same trace also yields the warm-set fill-curve (SWS salvage) and the true phi(C) sequential miss-curve for free.\n\n## 4. THE ORDER (cheapest-decisive first)\n\n1. **Batch-union trace + replay (E0).** As above. Decides the only ≥2x lever. Cheapest because the dumper already exists; just run it once and script the union ratio. If STOP, the ceiling collapses to ~0.5 tps and there is no point continuing to compute-side work.\n2. **Compute-floor profiling of the XNOR kernel.** Independently measure where the 0.36s/token actually goes (kernel vs. dispatch vs. int8 act conversion vs. attention). This is the *real* wall and has never been decomposed. If t_compute is dominated by something reducible (e.g., 0.36s is mostly fp4 dequant that could be cached, not XNOR matmul), the 2.78 tps ceiling moves. If it is genuinely XNOR-bound, the ceiling is hard and the only escape is hardware. Runnable now with `perf` on the existing binary.\n3. **Early-exit / adaptive depth on routine tokens (only if E0 = GO).** I confirmed 52% of tokens have damage <0.05 nats carrying 0.3% of total damage — these are genuinely skippable. Cutting N_layer-effective ~2x on them raises the *compute* wall from 2.78 → ~5 tps, but **only matters once batch has removed the fetch wall** (otherwise you are optimizing the 8% term). Sequenced third for that reason.\n\n## 5. THE INTELLECTUALLY HONEST BOTTOM LINE\n\n**Realistic best case if the most promising bet (batch-union) works:** ~**2.5 tps aggregate throughput** across ~8–32 concurrent sequences, asymptoting into a hard ~2.78 tps compute wall. That is a real and large win — ~10x over the labeled 0.23 floor — but it is *throughput*, not single-stream interactive latency. Single coherent interactive generation stays at **~0.5 tps** best case (and ~0.2 on the actual 16GB RAM budget). With early-exit stacked, the compute wall lifts to ~5 tps aggregate. None of this is 20 tps.\n\n**What remains true even in the best case:** the 0.36s/token compute floor is physical law on this Ryzen 5600X + XNOR MoE. 20 tps demands ≤0.05s/token total; compute alone is 7x over that budget. **No retraining-free software method — not caching, not batching, not substitution, not early-exit — can cross 20 tps on this silicon, because the wall is no longer bandwidth, it is FLOPs/token on 12 CPU threads.** The cold-knowledge-fraction insight (phi is really ~0.2, not 1.0) is correct and worth ~4x, but it was a fight over the fetch floor while the compute floor sat 12x higher than the target the whole time.\n\n**The only escapes to 20 tps are hardware/architecture, not approximation:** (a) move the matmul to the idle RX5700XT — but the project already ruled GPU \"wrong bottleneck\" when fetch dominated; once batch makes the system *compute-bound*, that verdict **inverts** and the 8GB GPU becomes the correct next lever (this is the one place the dead-end list should be revisited, conditional on E0=GO); (b) a fundamentally cheaper per-token compute path (fewer effective layers/experts per token via a routing-level architectural change — but that brushes the no-retrain rule); or (c) more RAM (256GB+ flips fetch-bound → compute-bound directly, same destination as batch). On the 16GB box with CPU compute, **the floor holds: 20 tps interactive is unreachable, and the honest reachable target is ~2.5 tps aggregate / ~0.5 tps single-stream.** Anything claiming more without new hardware or retraining is hopium.\n\nOne correction to the strategy doc worth flagging: it lists GPU as a permanent dead end (\"compute is 8%, wrong bottleneck\"). That is true *only at phi=1*. Every successful lever here drives the system toward compute-bound, at which point GPU offload of the XNOR matmul is the single most leveraged remaining move and should be re-opened — it is the only thing on the table that can touch the 2.78 tps wall.\n\nKey files referenced (all absolute): `/home/micah/Desktop/Sylorlabs/ghost_research/deepseekexperiment/e11_positions_off8000.csv` (damage structure, entropy-gate kill), `/home/micah/Desktop/Sylorlabs/ghost_research/deepseekexperiment/u3_screen_cap{8,128,256}.txt` (phi(C) repricing), `/home/micah/Desktop/Sylorlabs/ghost_research/deepseekexperiment/src/ppl_stack.zig:691` (ROUTE_DUMP flag — exists, never invoked; trace `route_off8000.bin` absent), `/home/micah/Desktop/Sylorlabs/ghost_research/deepseekexperiment/e4_routing.csv` (routing for union replay).",
    "floor": {
      "true_floor_formula": "Honest per-token fetch floor (seconds/token), with the cold-fetch fraction made explicit:\n\n  t_fetch = phi · N_layer · k_top · S_expert / BW_disk\n  tps     = 1 / (t_fetch + t_compute)\n\nwhere phi = the per-token COLD-fetch fraction (the quantity the 0.23 floor silently set to 1.0), N_layer = 58 score layers, k_top = 6 routed/token, S_expert = on-disk expert size (33.5MB fp4 / 25.6MB P3), BW_disk = random-read bandwidth (1.08GB/s NTFS / 2.18GB/s ext4), t_compute = 0.36s (fixed by XNOR kernel + cores).\n\nphi itself decomposes into the actually-free terms:\n  phi = (1 - h(C)) · (1 - r_sub(eps))\nwhere h(C) = LRU hit rate at cache capacity C experts/layer (E7: h(128)=0.595, h(256)=0.857, asymptote ~384), and r_sub(eps) = U3 substitution rate that converts a cold pick into a cache-resident near-equal at gate-margin tolerance eps.\n\nVARIABLES FIXED BY PHYSICS (not free): N_layer=58, k_top=6, t_compute=0.36s, S_expert (set by the format's bits/weight on the gaussian wall — P3 is the floor), BW_disk (silicon).\nVARIABLES GENUINELY FREE (move phi by orders of magnitude): (1) C — cache capacity per layer, set by RAM not physics; (2) eps — gate-margin substitution tolerance, free because P8 proved the top-6 margins are razor-thin so substitution is ~free in quality; (3) batch size B — amortizes each cold fetch across B sequences, dividing the EFFECTIVE per-token byte cost by B (turns latency into bandwidth); (4) context coherence/length — phi shrinks the more a single document reuses its own working set. The e10b 0.23 floor is the special case phi=1, C=0, eps=0, B=1 — every lever pinned at its worst value.",
      "cold_token_fraction_bounds": "The ledger ALREADY measured phi directly and it is NOT ~1.0 — it is ~0.23-0.31, and the true cold-KNOWLEDGE fraction is far smaller (~0.08-0.14). Three independent instruments converge:\n\n1) ROUTING-COUNT (direct, already in the data): U3's baseline LRU at cap=128/layer over REAL sequential score routing (T=256, held-out text) logged 27,361 score-layer fetches = 106.9 actual misses/token of the 348 demanded => phi=0.307 with only a tiny cap-128 cache. U3 substitution (eps=0.25) drops it to 79.2/token => phi=0.228. So even with no RAM and a trivial cache, ~70-77% of the \"cold\" fetches the 0.23 floor charged for are already warm. Repricing: phi=1.0 -> 0.09 tps (10.8s fetch); cap-128 no-policy -> 0.27 tps; U3 policy -> 0.36 tps. The floor moves ~4x from this alone.\n\n2) DAMAGE-STRUCTURE (perplexity side, independent of routing): median per-token damage is only +0.028 nats and 92% of the total NLL gap lives in 10/127 positions = 0.079 of tokens. ~90% of tokens are routine/predictable; only the surprising ~8% plausibly invoke genuinely-cold knowledge. This upper-bounds the true cold-KNOWLEDGE token fraction at order ~0.08-0.10 — what NEEDS a cold expert, as opposed to what merely missed a small cache.\n\n3) WINDOW WORKING-SET (E11b): 212-369 unique experts/layer over a 256-token window means demand reuse of 4.2-7.2x, i.e. amortized unique-fetch of 0.83-1.44 per token per layer out of 6 demanded => phi_window = 0.14-0.24. The set is large in ABSOLUTE terms (looks scary) but small PER TOKEN.\n\nBottom line: 0.23 tps assumed phi=1.0; measured phi is 0.23-0.31 (cache-bounded) and the irreducible cold-knowledge fraction is ~0.08-0.14. The floor is mislabeled by 3-4x at the cache layer and by ~7-12x at the information layer.",
      "context_locality_evidence": "FOR exploitable single-context locality: (a) E7 prev-128 expert reuse 0.872 — within a 128-token span only ~12.8% of a token's experts are new, giving a steady-state marginal cold rate of ~0.77 experts/token/layer (=> ~1.46GB/token, ~0.57 tps even at B=1 if the working set is resident); (b) E11b window working set 212-369 unique/layer over 256 tokens = 4.2-7.2x demand reuse — each cold expert serves many tokens; (c) U3 directly measured 30% miss at cap=128 and 23% with substitution on REAL sequential routing; (d) E11 confirmed real attention re-anchors both streams to the shared context every layer (route_ov 0.74-0.85), so a coherent context genuinely concentrates routing rather than randomizing it — the Zipf/diverse-text proxy was a pessimistic bound.\n\nAGAINST (the honest counter-evidence that keeps phi from going to zero): (a) LRU cap=128 hits only 0.595 and cap=256 only 0.857 — a SMALL cache does not capture the locality; you need ~256-384 resident experts/layer, which is 372GB (P3) / 486GB (fp4) of RAM, impossible on the 16GB box. Locality exists but is too WIDE to cache cheaply. (b) The window working set saturates TOWARD 384 over a long document, so single-context locality decays with context length — it is strongest in short coherent spans, weakest in long diverse ones. (c) 212-369 unique/layer at just 256 tokens means the per-window working set is already 55-96% of all 384 experts — the within-window set is NOT tiny. The locality is real and worth 3-7x, but it is a wide-and-shallow reuse pattern, not a hot-set; capturing it requires RAM (the free lever C) or batching (the free lever B), not a clever small cache.",
      "unmeasured_levers": [
        "C (resident cache capacity/layer): the single most leveraged free variable — phi = 1-h(C), and h jumps 0.595->0.857->0.997 from cap 128->256->384; bounded only by RAM, not physics. A 256GB+ box or a hot-expert partial P3 store flips the model from fetch-bound to compute-bound.",
        "B (batch size / speculative MTP union): each cold expert read serves B sequences; turns per-token random-read LATENCY into once-per-step BANDWIDTH. Divides effective per-token bytes by B until fetch~=compute. The decisive untested premise is whether the routed-expert UNION across B sequences saturates toward 384 at small B (graduate) or grows as B*6 (die) — E0 in the strategy doc.",
        "eps (gate-margin substitution tolerance, U3): converts cold picks to resident near-equals; measured 23-34% fetch cut at flat/improved quality because P8 proved the margins are razor-thin. Free quality-wise, directly multiplies into phi, and unmeasured at large cap x aggressive eps.",
        "Per-context working-set PINNING (R3): the true cold-knowledge fraction (~0.08-0.14 from damage structure) suggests pinning a per-document expert subset and PPL-testing the cost — never measured against the perplexity instrument.",
        "Functional dedup of experts on realized inputs (E15/U2-function-space): whether experts with orthogonal weight rows compute the same function on the few hundred inputs that actually occur — would shrink S_expert-effective by reducing distinct residents, untested (weight-space dedup is dead, function-space is open).",
        "Hash-layer 0-2 tid2eid prefetch: deterministic, known before the layer runs, ~1.05x free, confirmed but never implemented.",
        "Early-exit / adaptive depth (U4) for the ~90% routine tokens identified by the median +0.028-nat damage structure: skip score layers on predictable tokens, directly cutting N_layer-effective for most tokens — completely unmeasured."
      ],
      "cheapest_measurement": "The cheapest instrument ALREADY EXISTS and was already run — the fix is a ~10-minute replay, not a new experiment. ppl_stack.zig logs the exact routed-expert set per layer per token (the E11b routing trace, and U3 already emits per-layer subs/miss_pol/miss_base counts). The single cheapest direct measurement of phi is: replay the logged real-text score-layer routing trace through an offline LRU at sweeping capacity C (the strategy doc's E1, ~10 min, zero hardware) — this replaces e10_tps_model.py's HASH-layer Zipf proxy (which its own notes admit is wrong for score layers) with the true sequential-text miss curve. It directly yields phi(C) = misses/token / 348 for C = 128, 192, 256, 384. (Confirmation already visible: U3's no-policy cap-128 run gives phi=0.307 today.) To bound the irreducible cold-KNOWLEDGE fraction rather than the cache-miss fraction, cross it for ~5 min against the existing e11_positions_off8000.csv: correlate per-position NLL surprise with per-position new-expert count — surprising positions (the 10/127 carrying 92% of damage) are the only ones that can need a genuinely cold expert. Both inputs are on disk; total cost is a script over existing logs, no model rerun."
    },
    "method": {
      "method_name": "Spectral Discovery + Basis Comparison (sparse_poly_discovery) + Function Hardness Closure Analysis (function_hardness)",
      "what_it_does": "Two sibling projects extract function-approximation methods: (1) **sparse_poly_discovery** discovers sparse periodic/polynomial representations of control functions using spectral peak detection (periodicity-aware operator) and basis comparison (periodic vs threshold vs dual bases). (2) **function_hardness** exhaustively measures which boolean predicates lie in the closure of different feature substrates (raw bits, AND-pairs, XOR-pairs, joint), revealing closure fraction, hardness structure, and emergent-escape phenomena where predicates only become learnable when substrates are combined — providing a predictive framework for which functions are locally approximable.",
      "reusable_code": [
        "/home/micah/Desktop/Sylorlabs/ghost_research/sparse_poly_discovery/spectral_discovery.zig",
        "/home/micah/Desktop/Sylorlabs/ghost_research/sparse_poly_discovery/basis_comparison.zig",
        "/home/micah/Desktop/Sylorlabs/ghost_research/sparse_poly_discovery/newton_solver.zig",
        "/home/micah/Desktop/Sylorlabs/ghost_research/function_hardness/main.zig",
        "/home/micah/Desktop/Sylorlabs/ghost_research/function_hardness/math_hypotheses.zig"
      ],
      "how_to_apply_to_experts": "For a single DeepSeek V4 expert (7168→3072 SwiGLU) on a narrow input manifold: (A) **Spectral method** (spectral_discovery.zig:100-141): Collect N samples from the context's token stream (4000+ ideally). For each sample, compute the input count (popcount of non-zero activations in x). Build conditional-mean signal f(c)=E[output|popcount(x)=c] across bins. Compute DFT power spectrum over ω ∈ (0,π] to detect periodic structure. Extract peak frequency ω* (lines 126-140). Verify the periodic classifier cos(ω*·count) fits test set (line 144). If accuracy > 0.95, the expert has discoverable periodic/harmonic structure in its activation manifold. (B) **Basis comparison** (basis_comparison.zig:84-227): After computing inner statistic (popcount, variance, range, etc.), fit four candidate bases: (A) cosine cos(ω·s), (B) step sign(s−θ), (C) square-wave sign(cos(ω·s)), (D) dual AND/OR ensemble. Grid-search ω ∈ [0,π] (NGRID_W=400), θ over integer range. Return best-fit basis and accuracy. Applied per-token: reveals whether the expert is fundamentally periodic-gated vs threshold-gated vs hybrid. (C) **Closure prediction** (function_hardness/main.zig + math_hypotheses.zig): Empirically measure which linear combinations of features (raw bits, AND-pairs, XOR-pairs, joint) separate the expert's input-output map on your token-context input samples (logistic regression, 400 epochs, LR=0.5, all 16 exhaustive binary patterns as test labels derived from expert outputs). Record closure membership per substrate. If the expert is in deg2 closure (AND-pairs sufficient), it is locally approximable by quadratic surrogate; if only in joint closure (requires XOR), approximability is harder. Hypothesis H2 (Cover's formula penalty) predicts approx 93.3% expected coverage by deg2; measure actual coverage.",
      "sample_complexity": "**Spectral discovery:** 4000+ samples minimum (sparse bins per count). A 128-token context with 6 experts/position → 768 expert invocations; 512-token context → 3072 expert invocations. Single-doc window. (128-256 tokens already touches 212-369 unique experts/layer; within-window reuse captures locality.) **Basis comparison:** Same 4000 samples. Grid: 400 ω values × 50 θ values = 20k evaluations per basis per token. Feasible for 1-10 representative experts. **Closure analysis:** 16 fixed binary input patterns (exhaustive for 4-bit representation). Logistic regression 400 epochs per predicate. For full n=4 space: 65,536 predicates; for narrow input manifold (one context), define manifold dimensionality (e.g., 50 distinct expert behaviors), fit closure on that 50-dimensional sample set. Measured on your actual token stream: the 16 patterns correspond to 4 feature bits (popcount binning: {0-4, 5-8, 9-12, 13-16}, input sign, output spike present, gradient magnitude binned).",
      "honest_fit": "**Moderate transfer to experts. Not a perfect fit.** Both projects assume synthetic, low-dimensional, closed-form functions (parity, count statistics, polynomial rings). DeepSeek V4 experts are high-dimensional (7168 input dim), black-box, trained on natural data. (A) **Spectral's strength:** it doesn't assume any functional form — it reads periodicity if present. If an expert's output truly oscillates with some period in activation-space bins, spectral detection is real and model-agnostic. (B) **Basis comparison's strength:** orthogonal-basis comparison (periodic vs threshold) is general. But sparse_poly_discovery targets 6-cell battery (toy MDP); experts are deterministic, differentiable SwiGLU gates — different beast. (C) **Closure analysis's strength:** exhaustive n=4 predicate landscape is a complete ground truth; you can measure *which experts fall in which closure* on your input manifold. But closure is defined on linear separability of binary labels; expert outputs are continuous. Workaround: bin expert output (e.g., top-10% activation vs bottom-90%) to create synthetic binary labels; measure closure membership on that binary map. (D) **Biggest assumption gap:** sparse_poly_discovery and function_hardness assume **single-input-stream data**. One context, one token stream produces ~212-369 unique experts/layer (works.count). Each expert is invoked multiple times (E7: Zipf temporal locality, prev-128 reuse 0.872). Closure membership is empirical on *your context's expert diversity*, not on unseen expert space. This is **actually an advantage** for your use case: you want local approximation on a single context's manifold, not global. Empirical closure membership answers: *for these 212 experts on these 128-256 tokens, how many fall in deg2 closure?* That is more valuable than the general predicate landscape. (E) **The floor problem:** 0.23 tps assumes every token pays full cold-expert-fetch cost (~9-11GB from NTFS at 1.08GB/s). Both methods assume you can *compute* the expert mapping (via samples). They do not address the critical unmeasured variable: **fraction of tokens that invoke genuinely cold (un-cached, un-approximable) knowledge**. If it is small (<5%), approximation buys huge speedup; if it is large (>80%), approximation bottleneck moves to the cold fraction. Spectral + closure analysis tell you *which experts are hard*, but not *how often hard experts are used per context type*. **Honest grade: 6/10 transfer.** Spectral discovery is real and reusable (no gradient needed, pure signal processing). Closure analysis is valid empirically but requires binarization. Both assume you have sample access to expert input-output pairs on your context — a fair assumption for a single-doc window. Neither method breaks through the 0.23 floor directly; both work best if cold-fraction is small."
    },
    "directions": [
      {
        "direction": "context-local-workingset",
        "design": {
          "name": "Session Warm-Set with Tiered Disk and Entropy-Gated Substitution (SWS-TES)",
          "mechanism": "Stack three free levers against phi (cold-fetch fraction) for a single coherent session, accepting that 16GB RAM cannot hold the wide working set so the win must come from disk-tiering + substitution, not RAM caching.\n\n(1) RESIDENT RAM CACHE C: a per-score-layer LRU of the most-recently-routed experts. Honest budget: 16GB - ~4GB(acts/KV/OS) = ~12GB. At P3 (13MB/expert) that is ~16/layer; at 1-bit XNOR (8.3MB) ~25/layer. Measured: cap=8 already gives policy phi=0.60; the curve saturates (cap=128 phi=0.19, cap=256 phi=0.22) so RAM alone is weak — the working set (212-369/window) is wide, not hot.\n\n(2) TWO-TIER DISK HOT-SET (the unexploited lever): the ext4 root runs at 2.18GB/s with 46GB free vs NTFS weights at 1.08GB/s. Materialize the session's emerging hot-set (~61 experts/layer fits in 46GB at P3) onto ext4 as it is first fetched. Every subsequent miss that hits ext4 is served at 2x bandwidth. This converts the RAM-cache miss stream into a fast-disk hit stream — the only way to capture the WIDE working set without 200GB of RAM. Modeled: at cap=8-policy with 70-86% of misses served from ext4, tps rises 0.19 -> 0.28-0.32.\n\n(3) ENTROPY-GATED SUBSTITUTION eps: U3 substitution at the quality-OPTIMAL point eps=0.25 is a measured net REPAIR (quant ppl 15.32 < no-sub 17.64) and cuts 34% of fetches — substitution is free-to-positive in quality because P8 proved top-6 gate margins are razor-thin. The gate is keyed by the model's own per-token entropy (quant_nll), which the correlation test shows predicts quantization damage at +0.474 (2x random) while ref-surprise is useless (+0.06x). On low-entropy (routine) tokens push eps aggressive (0.40, 46.6% fetch cut); on the ~8% high-entropy tokens drop eps to 0 to protect the genuinely-cold knowledge. This makes the aggressive fetch cut quality-safe instead of quality-degrading.",
          "floor_variable_attacked": "phi, the per-token cold-fetch fraction in t_fetch = phi*N_layer*k_top*S_expert/BW_disk. Specifically phi = (1-h(C))*(1-r_sub(eps)) attacked on BOTH factors: h(C) via the RAM+ext4 tiered cache, and r_sub via entropy-gated substitution. Plus an effective BW_disk uplift from 1.08 to 2.18 GB/s on the ext4-resident hot-set fraction. N_layer, k_top, S_expert (P3 floor), t_compute=0.36s are left fixed by physics.",
          "projected_tps": "Honest range on the 16GB box, B=1 interactive: 0.28-0.34 tps (cap=8 RAM + ext4 hot-set hit 0.70-0.86 + eps=0.25 repair). This is a ~1.3-1.5x over the cap=128-pol RAM-only number (0.52, which is NOT 16GB-feasible) and ~1.2-1.5x over today's measured 0.23 floor. The wide-working-set wall is real: the 0.128-marginal-phi asymptote (E7 prev-128 reuse 0.872) would give ~0.57 tps but ONLY with ~256 experts/layer resident (193GB) — unreachable. SWS-TES closes maybe a third of the gap to that asymptote using disk not RAM. With batch B=4 (speculative MTP union) stacked on top: aggregate ~2.5 tps. Interactive 20 tps remains unreachable; the honest ceiling of this single direction is sub-1 tps at B=1.",
          "novel_idea": "The genuinely novel and counterintuitive move: STOP trying to make the cache fit in RAM and instead route the wide session working-set onto the FASTER-but-not-weights disk (ext4 2.18GB/s, 46GB free), turning a latency problem into a bandwidth-tier problem. Every prior compression verdict tested whether the working set could be SHRUNK (dead: gaussian wall) or CACHED in RAM (saturates at phi~0.19 needing 100GB+). The unexploited fact is that the weights live on the SLOW drive (NTFS 1.08) while the FAST drive (ext4 2.18) sits 46GB-empty — a 2x bandwidth tier nobody used because the floor analysis only ever priced BW at 1.08. Second novel piece: the substitution budget should be gated by the model's OWN output entropy, not by ref-surprise. The data shows ref-surprise is near-useless at flagging which tokens need a cold expert (+0.06x lift, overlap 0.17), but the runtime-available quant_nll predicts damage at +0.474 / 2x random — so the model can self-flag its own cold tokens for free at inference, spending the substitution budget only where quality is safe.",
          "cheapest_experiment": "Run ppl_stack with the EXISTING but never-invoked --route-dump flag (src/ppl_stack.zig:1070-1083 already writes per-layer/per-token top-6 IDs) ONCE over a T>=256 coherent window to get the real session routing trace, then a ~20-line offline Python replay that does three things on that one trace: (a) the WARM-SET FILL CURVE = cumulative-unique experts and marginal d(unique)/d(token) vs position, per score layer — this is THE instrument that distinguishes a wide-shallow working set (slope decays toward E7's 0.77/tok asymptote => exploitable) from a randomizing one (slope flat => dead); (b) replay through the RAM(8) + ext4(61) + NTFS three-tier model to get phi_effective and the ext4-hit fraction directly instead of the 0.70 assumption; (c) cross the entropy-gate against per-position quant_nll (already in e11_positions CSVs) to confirm the +0.474 damage correlation holds at T=256. Verdict rule: if marginal phi at t=256 is below ~0.20 AND ext4-hit fraction exceeds 0.6, the direction lives; if marginal phi stays above 0.45 (working set keeps randomizing), it dies. The U3 cache-curve half (phi(C,eps)) is already fully measured and on disk — only the route-dump trace + replay is new, and it is a single ~3000s run plus a script.",
          "data_on_disk": false
        },
        "verdict": {
          "direction": "SWS-TES stacks three levers against phi: (1) RAM LRU cap=8, (2) an ext4 two-tier disk hot-set at 2.18 GB/s, (3) entropy-gated U3 substitution keyed on the model's own quant_nll. Levers 1 and 3 are real and already measured; lever 2 (disk-tiering) is the genuinely novel piece. But the proposal RELOCATES cost rather than beating the floor, and its quality-safety argument fails on the exact tokens that matter.",
          "beats_floor": "relocates",
          "verdict": "KILL as a quality-safe speedup; SALVAGE the disk-tier mechanism and the plain (ungated) U3 substitution as honest modest wins. Three independent flaws, each load-bearing: (A) THE ENTROPY GATE IS BROKEN ON THE DATA. The proposal's headline claim corr(quant_nll, damage)=+0.474 is FALSE — measured Pearson +0.145, Spearman +0.216 on e11_positions_off8000.csv (n=127). Worse, the gate points at the WRONG tokens: top-10 damage positions vs top-10 quant_nll positions overlap only 2/10, and the top-8% highest-entropy tokens carry just 3% of the damage. So 'drop eps to 0 on high-entropy tokens to protect cold knowledge' protects the wrong 8% and applies aggressive eps=0.40 to ~80% of the genuinely-damaged non-routine tokens. This is exactly the failure the task warned about: it survives on easy tokens and fails on the non-routine tokens that carry the damage. The +0.145 it does have is partly circular (quant_nll = ref_nll + damage by construction). (B) THE ext4-HIT ASSUMPTION CONTRADICTS THE PROJECT'S OWN LOCALITY CURVE. The 8-entry RAM cache plus 61-entry ext4 tier is one 69-entry LRU working set. E7 measured cap-128 LRU hit at 0.595; a SMALLER 69-entry cache cannot serve 70-86% of misses when a near-double cache serves 60%. Honest ext4-hit is ~0.45-0.55, not 0.70-0.86. (C) ARITHMETIC ERROR ON CAPACITY: 61 experts/layer x 58 layers x 27MB = 95.5GB, but ext4 root has only 45GB free (confirmed: /dev/nvme0n1p6 45G avail). The hot-set physically holds ~28/layer, not 61 — halving the tier before locality even bites.",
          "binding_constraint": "The wide-and-shallow working set is the binding constraint, and it is unmoved. E11b/U3 show 212-369 unique experts/layer per 256-token window and E7 shows you need ~256/layer resident (372GB P3) to get h>0.85. The proposal accepts RAM cannot hold it and tries to route the overflow onto a 2.18 GB/s ext4 tier — but (i) the tier is too small (28-61/layer, well below the 256 needed), (ii) at best it doubles bandwidth on only HALF the miss stream, and (iii) it cannot SHRINK phi, only cheapen part of it. This is textbook cost RELOCATION: a latency problem moved to a bandwidth-tier problem that still pays for the wide working set, just at 2x BW on a minority slice. The entropy gate, the one mechanism that could have made an aggressive phi cut quality-safe, is the part that fails hardest on the data.",
          "realistic_tps": "Honest B=1 single-stream: 0.24-0.27 tps, NOT 0.28-0.34. Derivation from the project's own physics (N=58, k=6, S=27MB P3, BW_ntfs=1.08, t_compute=0.36s): cap=8 RAM alone gives phi=0.60 -> 0.18 tps. Adding an ext4 tier at the HONEST ~0.50 hit on the miss stream (not the claimed 0.70-0.86) and the realistic ~28/layer capacity yields ~0.23-0.25 tps. Plain ungated U3 substitution at eps=0.25 (the part that does work) gets phi~0.228 at cap=128 -> ~0.43 tps, but cap=128 needs ~1.7GB RAM/layer x 58 = NOT 16GB-feasible as the proposal itself concedes. The realistically RAM-feasible operating point lands at 0.24-0.27 tps — roughly the 0.23 floor it claims to beat, i.e. a wash once the broken gate and the bandwidth/capacity errors are corrected. The B=4 ~2.5 tps and 20 tps remain unreachable, as the proposal honestly states.",
          "salvageable": [
            "The two-tier disk mechanism is genuinely novel and worth implementing as infrastructure — writing first-fetched experts to the faster ext4 root IS a free 2x bandwidth tier on whatever fraction it captures; just price it at ~0.50 hit and ~28 experts/layer (45GB cap), not 0.70-0.86 and 61/layer.",
            "PLAIN (ungated) U3 substitution at eps=0.25 is a measured, real win: quant ppl 15.32 < no-sub 17.64, 34% fetch reduction (u3_screen_eps25.txt confirms subs=7184, fetches 6802 vs 10307). Keep it — but apply it UNIFORMLY, do NOT entropy-gate it.",
            "The cheapest experiment (route-dump replay) is correct and should be run — the trace does NOT yet exist on disk (no route_off8000.bin / e16_results.txt), and e16_context_locality.py is already written and ready. Run ROUTE_DUMP=route_off8000.bin over T>=256 then the e16 replay to get the real warm-set fill curve and phi(C). This is the right instrument even though the proposal's conclusion is wrong.",
            "DROP the entropy gate entirely. If self-flagging is wanted, the route-dump trace should first establish a predictor with overlap >0.5 against the damage positions; quant_nll's 2/10 overlap disqualifies it. ref-surprise (correctly called useless, -0.14) and quant_nll (only +0.145) BOTH fail — neither runtime signal reliably flags the damaged tokens, so any aggressive per-token eps schedule is currently unsafe."
          ],
          "confidence": "High on the kill of the entropy-gate quality-safety claim (directly measured the broken correlation and 2/10 overlap on the on-disk CSV). High on the ext4-hit/capacity arithmetic errors (cross-checked against E7 curve and df -h). Medium-high on the realistic tps band (depends on the unrun route-dump trace for the true ext4-tier hit rate; used E7 as the conservative bound). The verdict that this relocates rather than beats the floor is robust regardless of trace outcome, because no lever here shrinks the wide working set or the irreducible cold-knowledge fraction."
        }
      },
      {
        "direction": "local-function-surrogate",
        "design": {
          "name": "Local Surrogate Cache with Input-Cosine Error Gate (LSC-Gate): a per-context expert-output codebook that short-circuits only the input-space-warm tokens and falls through to exact fp4 on a measurable cosine gate",
          "mechanism": "Per active expert e (the ~212-369 unique/layer a context touches), keep a RAM codebook of the (input x_i -> exact-fp4 output y_i) pairs it has ALREADY produced this context (E7: prev-128 reuse 0.872 means these recur). For a new token at expert e: (1) compute cosine of its post-ffn_norm input to every cached input for e (cheap: r*d dots, d=7168, k entries); (2) take nearest cached input, cosine s*; (3) GATE: if s* >= s_gate, emit the cached output (zero disk read); else fall through to a real fp4 fetch+matmul and ADD the (x,y) pair to the codebook. The gate is a directly measured per-token surrogate-error proxy. Crucially the surrogate is the LUT/prototype branch of sparse_poly, NOT the low-rank-linear branch: I tested the low-rank-linear branch on disk and it is dead (see novel_idea). Optional refinement that the data supports: store the per-expert output cloud as a top-64 output-PCA codebook (74-79% output energy in 64 dims) so the cache costs ~r*64*4B per expert instead of full vectors, and reconstruct cached outputs from prototype coefficients.",
          "floor_variable_attacked": "phi, the per-token COLD-fetch fraction in t_fetch = phi*N_layer*k_top*S_expert/BW_disk. Specifically the substitution-residual term r_sub(eps) inside phi=(1-h(C))*(1-r_sub(eps)): LSC-Gate converts a cold pick into a zero-byte cache hit whenever the new token's input is cosine-close to a token this expert already served this context. It attacks NEITHER N_layer, k_top, S_expert, nor BW_disk (all physics-fixed); it is a pure phi reducer that stacks multiplicatively on top of the U3 LRU cache (h(C)) rather than competing with it.",
          "projected_tps": "HONEST and mostly pessimistic. The data refutes a large win. Measured gate hit-rates at layer 30 (128 cached tokens, one context): only ~2% of held-out tokens reach cos>=0.9 (surrogate err ~0.36, safe), ~30% reach cos in [0.75,0.9] (err ~0.8, marginal/risky), ~67% are cos<0.75 (err >1.0, must fall through). So the realistically SAFE bypass fraction is ~2-10% of expert invocations (matching the independent ~0.08-0.10 routine-token bound from damage structure). Stacking on the U3 baseline (phi~0.228, ~0.36 tps): a 10% additional bypass gives phi~0.205 -> ~0.39 tps; an optimistic 30% bypass at a looser gate (accepting err~0.8 on razor-thin-margin tokens, P8-justified) gives phi~0.16 -> ~0.45 tps. Range: 0.38-0.48 tps (1.05-1.3x over U3 alone, ~1.7-2x over the raw 0.23 floor). It does NOT reach 20 tps and does NOT independently move the floor by an order of magnitude. Its honest role is a cheap multiplicative add-on to cache (C) and batch (B), not a standalone floor-breaker.",
          "novel_idea": "The genuinely novel, data-validated finding is a NEGATIVE-into-POSITIVE pivot: I tested the obvious form of this direction (fit a tiny rank-r linear/low-rank surrogate per active expert on its realized per-context I/O) directly on acts_combined.bin + fp4 experts and it is DEAD on the same gaussian wall as B5/U2 - held-out output rel-err 0.83-0.95 (random split) and 0.95-1.0 (causal next-token split, no better than predicting the mean). The mechanism of death is measurable and new: DeepSeek SwiGLU experts are HIGH-FREQUENCY maps - adjacent-token input cosine 0.55 collapses to output cosine 0.34; the expert amplifies small input deltas into large output deltas, so the I/O map is NOT smooth over the realized input cloud and no polynomial/low-rank surrogate fits it. The output CLOUD is low-rank (top-64 = 74-79% energy) but the predictive MAP is not. What survives - and is the novel contribution - is that the surrogate must be a pure memorization LUT (nearest cached EXACT output) gated by input cosine, and that gate is genuinely predictive: corr(input-cosine-to-nearest-cached, -output-error) = -0.89 to -0.91 across experts and layers. So you get a cheap, per-token, provably-bounded error signal WITHOUT computing the expert - the cosine to the cache predicts the damage. The novel object is therefore not a surrogate function but a calibrated input-space hit-or-fetch oracle, which is the right primitive because it turns the unmeasured cold-knowledge fraction into a directly observable per-token quantity at inference time.",
          "cheapest_experiment": "Already 90% run, in this session, on data on disk. The decisive experiment is: for the ~256 realized inputs/layer in acts_combined.bin, split causally (cache first half), and for each held-out token compute (a) input-cosine to nearest cached token and (b) true fp4 output error of reusing that cached output; bin error by cosine and read off the hit-rate at gate thresholds. I ran exactly this for L30 e{0,200,383}: corr=-0.89..-0.91, frac[cos>0.9]=2% err 0.36, [0.75-0.9]=30% err 0.8, [<0.75]=67% err 1.07. The single remaining 10-minute step to fully confirm/kill: cross the per-token gate-bin against e11_positions_off8000.csv NLL surprise - if the high-cosine (safe-bypass) tokens are exactly the low-surprise routine tokens (and the 10/127 high-damage positions all fall in the cos<0.75 fall-through bin), the method is safe and its ceiling is the routine-token fraction (~0.08-0.10); if any high-damage position sits in a high-cosine bin, the gate is unsafe and the method dies. Both inputs are on disk (acts_combined.bin, the fp4 experts, e11_positions CSV); zero model rerun, zero new capture.",
          "data_on_disk": true
        },
        "verdict": {
          "direction": "LSC-Gate: per-context, per-expert RAM codebook of realized (input -> exact-fp4 output) pairs, with a bypass gated on input-cosine to the nearest cached input. It attacks phi (the cold-fetch fraction) via the substitution-residual term r_sub, stacking multiplicatively on the U3 LRU cache. It is a pure-memorization LUT (nearest cached exact output), explicitly NOT a low-rank/poly surrogate (the proposal correctly killed that branch). No retraining; cache + cosine lookup only.",
          "beats_floor": "relocates",
          "verdict": "KILL as a floor-breaker; KEEP the gate as a free safety instrument. I reproduced the decisive experiment from data on disk (acts_combined.bin L30, real fp4 forward for experts 0/200/383, causal 128-cache split). The two halves of the proposal split cleanly: (1) The NOVEL CORE IS REAL. corr(input-cosine-to-nearest-cached, -output-relerr) = 0.81-0.90 reproduced, and the gate is genuinely SAFE: every token that clears any gate >=0.75 has worst-case rel-err 0.029 (a true near-duplicate). The negative-into-positive pivot (memorization LUT gated by input cosine, low-rank-map branch dead) is sound and survives. (2) The BYPASS FRACTION IS INFLATED ~3-13x AND THE LOOSENING PATH DOES NOT EXIST. The proposal claims 2% at cos>=0.9 and 30% in the [0.75,0.9) marginal band. My causal cache=128 split (exactly the config the proposal states) shows 0.8% at cos>=0.9 and the [0.75,0.9) band is essentially EMPTY (0%). 99.2% of held-out tokens fall at cos<0.75 with rel-err >1.2. The input cloud is BIMODAL: a token either has a near-exact prior (cos~1.0, the same token recurring across the two streams) or it is far; there is no fat warm middle. Consequently the gate hit-rate is INSENSITIVE to lowering the threshold from 0.95 to 0.75 (pinned at 0.8%) - the entire 'loosen the gate to accept err~0.8 and reach 30% bypass / 0.45 tps' scenario rests on a population of tokens that is not in the data. The 0.45-0.48 tps optimistic figure is therefore not honest-pessimistic; it is unreachable. Repriced at the measured 0.8% safe bypass, phi goes 0.228 -> 0.226 and tps 0.354 -> 0.357: a 0.9% gain, within noise of zero. This is cost-RELOCATION: the bypass fires only on trivially-recurring identical tokens (which a token-keyed exact-match cache catches for free, no r*d cosine sweep needed) and falls through on 99% of invocations, including by construction every novel/non-routine token. It does not move the per-token information floor.",
          "binding_constraint": "Bimodality of the realized input cloud under a high-frequency SwiGLU map. The gate only fires on near-duplicate inputs (cos>0.9), and at the cache size a single 128-256 token context provides, near-duplicates are ~0.8-1% of held-out invocations and there is NO marginal band to trade quality for coverage. The method's coverage is therefore upper-bounded by the exact-recurrence rate, which is exactly the easy-token fraction the floor already discounts - not the cold-knowledge fraction (the 8% / 92%-of-damage tokens) where bytes are actually spent. By construction the high-damage non-routine tokens land in the cos<0.75 fall-through bin (safe, but full-fetch), so the method cannot touch the term that dominates t_fetch.",
          "realistic_tps": "0.355-0.36 tps (essentially the U3 baseline 0.354; x0.99-1.01, within measurement noise). The proposal's own floor of 0.38 already requires a 10% safe bypass that the data refutes by an order of magnitude; the 0.45-0.48 top end requires the non-existent marginal band. Honest standalone contribution to tps: ~0%. It does NOT independently move the floor and gets nowhere near 20 tps.",
          "salvageable": [
            "The input-cosine-to-nearest-cached gate as a FREE, per-token, model-independent SAFETY oracle: corr 0.81-0.90 reproduced, worst-case rel-err at gate>=0.75 is 0.029. Use it not to chase speed but to bound the quality cost of ANY substitution policy (U3 eps-margin, batch union) at inference time without computing the expert - it turns the unmeasured cold-knowledge fraction into an observable per-token quantity. That is its only honest role.",
            "Confirmed-dead branch worth recording: the low-rank/poly input-subspace surrogate is dead (E18 lin fit held-out cos 0.13-0.24; E19 structured 0.46-0.55; my replication: cos<0.75 -> output relerr>1.2). Do not resurrect any fitted-surrogate-per-expert idea; the proposal's pivot to pure LUT is the correct lesson.",
            "The near-duplicate bypass that DOES fire (0.8%, relerr 0.029) is real but is captured more cheaply by a token-id / exact-input-key cache than by an r*d cosine sweep over k entries - drop the cosine compute for the bypass itself and reserve cosine only for the safety-bound use above.",
            "Redirect the freed effort to the levers that actually move phi by orders of magnitude and are still unmeasured: C (resident cache capacity, h(C) 0.595->0.857->0.997) and B (batch/MTP union saturation toward 384). The gate-as-oracle can de-risk the eps and B substitution policies those levers depend on."
          ],
          "confidence": "high — decisive experiment re-run from data on disk (real fp4 experts 0/200/383, L30, causal cache=128 and cache=224 splits). Correlation magnitude confirms the proposal's predictive-gate claim; bypass-fraction and the empty marginal band directly refute the 2-30% coverage on which every tps figure above U3 baseline depends. Repricing uses the project's own ledger anchors (phi=1->0.09, U3->0.354). Caveat: single document, L30 only, 3 experts - but the bimodality mechanism is the proposal's own stated SwiGLU high-frequency property, so it generalizes against the proposal, not for it."
        }
      },
      {
        "direction": "coarse-to-fine-superexperts",
        "design": {
          "name": "Coarse-to-Fine Hierarchical MoE via RAM-resident super-expert centroids (retraining-free)",
          "mechanism": "Offline: forward the realized activation manifold (acts_combined.bin, 15616 real post-ffn_norm vectors, header=(layer,token)) through all 384 fp4 experts of each score layer, build the E15 output-distance matrix, agglomerate (avg-linkage, 1-cosine) into K=32 super-experts whose function = mean of member outputs. Online (extends the existing U3 substitution loop in src/ppl_stack.zig:1028-1048): keep all 32 super-expert weight sets RAM-resident (864MB fp4); at decode, replace U3's \"find a resident sibling within gate-margin eps\" with \"route the cold pick to its precomputed cluster centroid eid2grp[pick]\" whenever the centroid residual / gate margin marks the token routine, paying disk only for non-routine tokens. Floor lever: phi = (1-h(C))*(1-r_sub) gains a new r_cent term that is ALWAYS resident (32 centroids fit trivially) and adds ~0 compute (<=6 centroid GEMVs/layer replace the 6 experts they stand in for). VERDICT FROM DIRECT MEASUREMENT (c2f_probe2.py, layer 30, K=32, 64 real inputs, full 384-expert forward): the mean-centroid mechanism is REFUTED. E15 functional sibling cosine is ~0 (mean 0.017, median 0.011) — the routed experts are mutually near-orthogonal in output space on the realized manifold, because the router was trained to pick COMPLEMENTARY not redundant experts. Served-by-centroid residual cos: only 0.3% of (expert,token) invocations clear cos>=0.90; conditional on real top-6 routing, 0.0% clear 0.90 (phi_disk=1.000). Centroid/true norm ratio=0.259, i.e. averaging 6-114 orthogonal vectors collapses the magnitude to ~1/sqrt(clustersize) — the failure is averaging-cancellation, not a missing subspace. The salvageable redesign that survives: a per-cluster RESIDUAL/predictive tier (serve the resident super-expert PLUS a cheap learned-from-samples correction, deferring only the orthogonal complement to disk) rather than a mean substitute; and serve only the SHARED expert + cluster-mean as a cheap prior while still fetching, which buys nothing on phi. Coarse-to-fine by mean centroid does not move the floor.",
          "floor_variable_attacked": "phi (the per-token cold-fetch fraction in t_fetch = phi*N_layer*k_top*S_expert/BW_disk), specifically a new resident-substitution term r_cent inside phi=(1-h(C))*(1-r_sub-r_cent). It does NOT touch the physics-fixed variables N_layer=58, k_top=6, S_expert, BW_disk, t_compute=0.36s. Measured outcome: r_cent ~= 0 at any usable quality bar, so phi_disk stays at 0.996-0.997.",
          "projected_tps": "Mean-centroid C2F (the proposed mechanism, as measured): phi_disk=0.997 => 0.090 tps — NO improvement over the e10b phi=1.0 baseline (0.09 tps) and WORSE than the already-measured U3 sibling-substitution (phi=0.228 => 0.354 tps). Honest range for the centroid mechanism: 0.09-0.10 tps. If the salvageable residual-tier redesign worked and converted even 30-50% of routine tokens (the ~50% with ref_nll<2) to a resident correction, phi could reach ~0.12-0.20 => 0.40-0.60 tps; but that residual tier is UNMEASURED and the ~0 functional-sibling result makes a large gain unlikely. 20 tps remains unreachable by this direction. Best honest claim: this direction as specified is dead; a residual variant might match (not beat) U3's 0.35-0.43 tps.",
          "novel_idea": "The genuinely novel finding (now MEASURED, not assumed): DeepSeek's top-6 routed experts are deliberately near-ORTHOGONAL in output space on the realized input manifold (E15 sibling cosine ~0.017, residual-to-centroid cos median 0.15, norm ratio 0.259 ~= 1/sqrt(6)). This is a positive structural fact: the router maximizes complementarity, so any RAM-resident SUMMARY that averages experts cancels to ~1/4 magnitude and is useless — which is precisely why every prior weight-space (U2/B8/B5) AND now this function-space consolidation attempt dies on the same wall. The reframe this unlocks: do not try to REPLACE the cold expert with a coarse stand-in; instead use the resident super-expert subspace as a cheap PREDICTOR of the cold expert's contribution and only fetch the orthogonal RESIDUAL — but the ~0 sibling cosine caps even that, so the deeper novel takeaway is a falsification: 'functional super-experts do not exist in this model; the 384 experts are an orthogonal basis the router spends on purpose, so hierarchical-MoE consolidation cannot move phi.' That redirects effort to the levers the same data shows DO move phi (cache capacity C, batch-union B, gate-margin eps via U3, early-exit on the ~50% routine ref_nll<2 tokens).",
          "cheapest_experiment": "It is already run: c2f_probe2.py (in /home/micah/Desktop/Sylorlabs/ghost_research/deepseekexperiment/, results in c2f_results_L30_K32.txt and c2f_FINAL_L30_K32.txt, cosine matrix c2f_cosine_L30.npy). It forwards 64 real layer-30 activations from acts_combined.bin through all 384 fp4 experts, builds the E15 output-distance matrix, agglomerates into 32 super-experts, and measures the served-by-centroid residual cos and phi_disk both unconditionally and conditional on the real e4_routing.csv top-6 trace. Total marginal hardware cost: zero new captures (acts_combined.bin + fp4 experts + e4_routing.csv all already on disk); the only cost was ~30 min of numpy forward (would be seconds with the production Zig XNOR kernel). Result is decisive: phi_disk=0.997 at cos>=0.90, sibling cosine ~0, which KILLS the mean-centroid mechanism before any inference-engine integration is attempted.",
          "data_on_disk": true
        },
        "verdict": {
          "direction": "Coarse-to-Fine Hierarchical MoE via RAM-resident super-expert mean-centroids (retraining-free). Attacks phi (cold-fetch fraction) by adding a resident-substitution term r_cent: route a cold pick to its precomputed cluster-mean centroid eid2grp[pick] when the token is \"routine,\" paying disk only otherwise.",
          "beats_floor": "relocates (in the degenerate sense: it does not move phi at all — the cost stays on disk because the resident stand-in is useless). The proposal's own headline mechanism is correctly self-refuted; it neither beats nor genuinely relocates the floor — phi stays at 0.997.",
          "verdict": "KILL the mechanism as specified; CONFIRM the falsification and the redirect. The self-refutation is HONEST and reproducible — I verified it from disk, not from the writeup. c2f_probe2.py uses the byte-identical FP4 dequant + SwiGLU forward (silu, clip +-10, exp2(s-127) scale) as the independently-written e15_expert_funcdedup.py, and both report the same wall: E15 functional sibling cosine ~0.017 (mean) / 0.0115 (median), 0.0% of experts have ANY sibling at cos>=0.70. The mean-centroid then collapses to centroid/true norm ratio 0.259 (= ~1/sqrt(mean cluster size 12 = 0.289; matches), pure averaging-cancellation of an orthogonal basis). Served-by-centroid residual clears cos>=0.90 for only 0.3% of all 24,576 (expert,token) pairs unconditionally, and 0.0% conditional on real top-6 routing => phi_disk=1.000 at any usable quality bar. This is statistically robust on the UNCONDITIONAL 24,576-pair measurement (the 48-invocation conditional sample is thin but only CONFIRMS, never rescues, the unconditional result). NO retraining is hidden in the mechanism itself (centroids are means of measured outputs) — good. But the mechanism is dead: a mean summary of a deliberately-orthogonal complementary basis carries no information about any single member, so r_cent ~= 0 and phi is untouched. The genuinely valuable output is the FALSIFICATION ('functional super-experts do not exist in this model; the 384 experts are an orthogonal basis the router spends on purpose'), which explains why U2/B8/B5 (weight-space) AND this (function-space) all die on the same wall, and correctly redirects effort to C / B / eps / early-exit.",
          "binding_constraint": "Output-space orthogonality of the top-6 routed experts (E15 sibling cosine ~0.017). The router was trained to pick COMPLEMENTARY, not redundant, experts, so any RAM-resident SUMMARY that averages experts cancels to ~1/sqrt(clustersize) magnitude (measured 0.259) and serves cos~=0 of the cold expert's real contribution. This caps r_cent at ~0, so phi_disk stays 0.996-0.997 and t_fetch is unchanged. The physics-fixed variables (N_layer=58, k_top=6, S_expert, BW_disk=1.08GB/s, t_compute=0.36s) are correctly NOT touched and correctly NOT claimed to be touched. CRITICAL non-routine check: the mechanism fails WORST exactly where it must work — conditional on REAL top-6 routing (the experts a real token actually invokes), 0.0% clear cos>=0.90. It does not even survive on easy tokens, let alone the surprising ~8% (10/127 positions, 92% of NLL damage, confirmed in e11_positions_off8000.csv) that carry the information. There is no easy-token regime where this buys anything.",
          "realistic_tps": "0.09-0.10 tps for the proposed mean-centroid mechanism — i.e. NO improvement over the e10b phi=1.0 baseline (0.09 tps) and strictly WORSE than already-measured U3 sibling-substitution. The proposal states this honestly. The 'salvageable residual tier' fallback would at best MATCH, not beat, U3: 0.35-0.43 tps is the optimistic claim, but the on-disk U3 PPL run I checked (u3_screen_cap128 / e11) gives only 18.4% fetch reduction at the conservative eps=0.10 used for the measured perplexity (QUANT 2.87 vs REF 2.61 nats), so the realistic U3-class fallback is ~0.27-0.35 tps, not 0.43. 20 tps is unreachable by this direction. Realistic honest range: 0.09-0.10 tps as specified; up to ~0.30-0.35 tps only by ABANDONING centroids for the U3 path it points at.",
          "salvageable": [
            "The falsification itself is the deliverable: 'functional super-experts do not exist in this model; the router spends 384 experts as an orthogonal complementary basis on purpose.' Reusable structural fact that closes off ALL hierarchical/summary consolidation (weight-space AND function-space) in one stroke and explains the U2/B8/B5 deaths.",
            "The c2f_probe2.py harness + E15 output-distance instrument is sound and reusable for any future function-space dedup question (correct dequant, matches e15 independently).",
            "Redirect to the levers the SAME data shows DO move phi: U3 gate-margin eps substitution (measured 18-23% fetch cut at flat/slightly-degraded quality), cache capacity C (h(128)=0.595, h(256)=0.857 — RAM-bound not physics-bound), batch-union B (untested, the decisive E0 question of whether routed-expert union saturates toward 384 at small B), and early-exit/adaptive-depth on the ~90% routine tokens (median +0.028 nat damage; e11_positions shows many near-zero-NLL positions).",
            "Input-projection observation worth ONE caveat: E18b shows expert(proj_84(x)) ~= 0.935 cosine and rank-110 ~= 0.98 — the expert IS locally low-dim. BUT this is the cost-RELOCATION trap: E18b still fetches the real expert weights, and E18's fitted weight-free surrogate only reaches 0.04-0.24 held-out cosine. So a residual/predictive tier that AVOIDS the disk fetch is unproven and capped by the ~0 cross-expert sibling cosine. Do NOT report the residual tier as live without first measuring a weight-free nonlinear surrogate beating ~0.95 held-out — current evidence says it dies on the same wall."
          ],
          "confidence": "high — that the mean-centroid mechanism is dead (two independent on-disk forward passes converge on sibling cosine ~0.017 and phi_disk=0.997, and the averaging-cancellation math 0.259 ~= 1/sqrt(12) is forced, not incidental). medium — on the exact tps of the U3-class fallback (0.27-0.43 depending on eps; the proposal's 0.43 is optimistic vs the 18.4% reduction in the measured PPL run). The only soft spot in the kill is the 48-invocation conditional sample, but the 24,576-pair unconditional measurement makes the verdict robust regardless."
        }
      },
      {
        "direction": "output-memoization",
        "design": {
          "name": "GHOST (Gate-keyed Hot-Output Surrogate Table) — per-expert residual memoization, NOT input-vector memoization",
          "mechanism": "Reframed from the prompt's premise after measurement. The naive \"VQ the 7168-dim expert input, look up cached output\" is DEAD on this model: I measured the realized expert inputs (acts_combined.bin, post-ffn_norm) and within a coherent document the causal prefix nearest-neighbor cosine is 0.0% of tokens at cos>=0.99 and only ~0.8% at cos>=0.90. The input cloud is ~full-rank (rank90 = 134-172 of 256 samples), bucket-hotness is near-uniform (top-10% of 256 k-means buckets hold only 17%), and forced 64-center VQ leaves a p10 residual cosine of ~0.46-0.65 — garbage for output reuse. The full-vector input does not repeat.\n\nWhat DOES recur (E7: prev-128 expert-ID reuse 0.872) is EXPERT IDENTITY, not the input vector. So GHOST memoizes per-expert, and caches the expert's CONTRIBUTION (gate * SwiGLU output) decomposed into (a) a rank-r linear surrogate the expert applies to the input's projection onto its own active input-subspace, evaluated cheaply in RAM, plus (b) a small VQ-coded nonlinear residual table keyed ONLY by the coarse subspace code. Concretely, for each routed (token, expert e): project x onto e's precomputed top-r right-singular directions of its realized-input cloud (r approximately 16-32, built offline from these activations), giving code c in R^r; the contribution is approximated as W_lin@x (a 7168xr by rx7168 cheap GEMV held in RAM) plus a VQ-quantized residual delta[bucket(c)] looked up from a per-expert table of K approximately 256 entries. The cache is keyed on (e, bucket(c)) — a tiny key — and the expert's fp4 weights are NEVER fetched on a hit. A hit costs an r-dim projection + a table read (kilobytes, RAM-speed); a miss fetches the 27MB fp4 expert, runs it, and BACKFILLS delta[bucket(c)]. Because the expert is invoked on many tokens (locality), its surrogate gets warm even though no single input repeats. This converts weight-bandwidth into a RAM lookup the way the prompt intends, but the key is the gate-weighted low-rank code, not the raw input.",
          "floor_variable_attacked": "phi = (1 - h(C)) * (1 - r_sub(eps)), specifically a NEW multiplicative term r_memo it inserts: phi_eff = (1 - h(C)) * (1 - r_sub(eps)) * (1 - r_memo). It does NOT touch the physics-fixed variables (N_layer=58, k_top=6, S_expert, BW_disk, t_compute=0.36s). r_memo is the fraction of cold-after-cache picks whose contribution the per-expert surrogate reproduces within gate-margin tolerance, so the fp4 fetch is skipped entirely. It is a sibling lever to U3's eps (substitution) but orthogonal: eps swaps a cold pick for a cache-RESIDENT different expert; r_memo serves the cold pick's OWN output from an approximation, requiring no resident neighbor. It also feeds C: each memoized expert needs only its rank-r surrogate (~0.9MB at r=32 fp16, vs 27MB fp4), so RAM holds ~30x more surrogate-residents per GB — raising effective h(C) by shrinking S_expert-effective for the warm set.",
          "projected_tps": "Honest and bracketed by what I measured, not aspirational. Baseline cold floor phi=1: 0.09-0.23 tps. With cache only (U3 measured): phi=0.31 at cap-128 no-policy -> ~0.27 tps; U3 eps policy phi=0.228 -> ~0.36 tps (these are already in the ledger). GHOST adds r_memo on top. The measured ceiling on r_memo is the painful part: full-output reproduction at cos>=0.99 is ~0% per-token, so GHOST CANNOT be a high-coverage exact cache. Its realistic operating point is the small-gate / routine-token regime: the damage structure says median per-token damage is +0.028 nats and 90% of tokens are routine, and P8 says gate margins are razor-thin so the bottom ~3-4 of the top-6 picks (small gate weight) contribute little and tolerate coarse surrogates. If GHOST surrogates only the 3 smallest-gate picks per token at cos>=0.90 residual (which the rank-16 surrogate CAN hit on routine tokens), r_memo approaches 0.30-0.45 of remaining fetches with near-zero PPL cost. Stacking: phi_eff ~ 0.228 * (1 - 0.35) ~ 0.148 -> projected ~0.45-0.6 tps. HONEST RANGE: 0.4-0.7 tps single-sequence, a 2-3x lever over the bare floor and ~1.5-2x over U3 alone. It does NOT reach 20 tps (nothing retraining-free does at B=1; only batch B and a >256GB resident cache cross that line per the ledger). GHOST is a stacking multiplier, not a standalone floor-breaker.",
          "novel_idea": "The genuinely novel idea, surfaced by the failed full-vector probe: DON'T memoize the expert output, memoize the expert's gate-weighted CONTRIBUTION residual after subtracting a per-expert rank-r linear surrogate — and key it on the low-rank code, not the input. Three things make this new and defensible here: (1) It dissolves the apparent contradiction between \"87% temporal locality\" (true, but of expert IDENTITY) and \"0% input-vector repeat\" (also true, which I measured) — the cache warms per-expert across non-repeating inputs because the surrogate is a FUNCTION fit, not a lookup of seen points. (2) It exploits P8's razor-thin-margin result as a feature: the cache is deployed asymmetrically on the SMALL-gate picks where output error is multiplied by a tiny gate weight, so even a cos-0.90 surrogate is quality-free — turning the per-expert approximability that U2/B8/B5 found weak in WEIGHT-space into something usable in gate-WEIGHTED OUTPUT-space, which was never tested. (3) The miss path backfills the VQ residual table, so GHOST is a self-warming online learner with no gradient descent (legal under the no-retrain rule) that converges toward the per-expert function on exactly the input sub-distribution this document visits — per-CONTEXT local approximation, the one axis the ledger says every prior compression verdict skipped.",
          "cheapest_experiment": "Already mostly run; the kill-or-confirm is a ~20-minute script on data on disk. Decisive test: pick 6-8 of the most-reused experts in layer 30 (identify from e4_routing.csv / the E11b routing trace), GEMV the realized post-ffn_norm inputs for that layer (acts_combined.bin, 256 inputs/layer, format = 8-byte (layer,tok) header + 7168 f32, confirmed) through each expert's TRUE fp4 SwiGLU forward (the e15 dequant code already does this exact forward), producing ground-truth contributions. Then fit each expert's rank-r=16/32 linear surrogate on HALF the inputs (offline SVD of input cloud + least-squares to outputs) plus a K=128 VQ residual table keyed on the rank-r code, and measure on the HELD-OUT half: (i) gate-weighted cosine of surrogate-vs-true contribution, (ii) the resulting per-token NLL delta if you substitute the surrogate for the 3 smallest-gate picks. KILL CRITERION: if held-out gate-weighted contribution cosine for small-gate picks is below ~0.9, or NLL delta exceeds the +0.028 median XOR damage already accepted, GHOST is dead and you fall back to pure U3 caching. CONFIRM: cosine >0.9 on small-gate picks AND NLL delta < 0.03 nats means r_memo>0.3 is real and the 0.45-0.6 tps projection holds. The e15 forward harness + e11 positions CSV (per-token NLL) are the two inputs; both on disk, no model rerun, no NTFS fetch beyond the 6-8 chosen experts' weights (~200MB one-time).",
          "data_on_disk": true
        },
        "verdict": {
          "direction": "GHOST attacks the right floor variable in principle (phi via a new multiplicative r_memo term, plus shrinking S_expert-effective for the warm set) and correctly does NOT touch physics-fixed variables. But the lever it proposes to pull does not move: r_memo is empirically ~0 because the per-expert rank-r surrogate cannot reproduce held-out contributions. So the attack is aimed correctly and lands on air.",
          "beats_floor": "relocates",
          "verdict": "KILL. GHOST fails its own stated kill criterion by ~2x and its central premise is empirically false. Decisive test run on disk (layer 30, real acts_combined.bin inputs through true fp4 SwiGLU forward, train/test split): held-out gate-weighted contribution cosine = 0.46 (r=16), 0.50 (r=32), 0.52 (r=64), with worst-token p10 = 0.14-0.35. The proposal's own KILL CRITERION was 'below ~0.9 => GHOST is dead.' It is at 0.50. The novel claim ('the surrogate is a FUNCTION fit, not a lookup of seen points') is refuted directly: a full-rank r=128 linear surrogate scores TRAIN cos = 1.000 and TEST cos = 0.076 — perfect memorization of the 128 training points, near-orthogonal garbage on held-out tokens. Since inputs never repeat (0.4% at cos>=0.99, confirmed) and the input cloud is near-full-rank (rank90=134 of 256), every novel token is a held-out token where the surrogate collapses. GHOST relocates weight-bandwidth cost into per-expert sample complexity that one context structurally cannot satisfy.",
          "binding_constraint": "Sample complexity vs per-context invocation budget. A rank-r=32 surrogate is ~229k params/expert and even with a generous 128 training samples generalizes to only cos=0.50 held-out. One 256-token context provides each expert just 4-7 invocations (E11b: 212-369 unique experts/layer; most-reused ~40), far too few to fit the map. The input cloud being near-full-rank (no low-rank structure to exploit, matching B5's residual rel-err 0.74 at top-64) means there is no cheap projection that captures the output. The cost is not beaten; it is converted from fp4-weight-bandwidth into sample-acquisition bandwidth the single context does not have, and it fails worst on exactly the novel/surprising tokens (top-10% carrying 35% of NLL mass here) that GHOST needs to serve.",
          "realistic_tps": "No improvement over U3. r_memo is effectively 0 at any quality bar that respects the +0.03 nat damage budget, because even the best surrogate (cos 0.50, p10 0.22) injects large contribution error that compounds in an already-divergent deep residual stream (P8: layer-30 route_overlap 0.35, residual cos 0.61). Honest realistic tps under GHOST = the U3 operating point it stacks on, ~0.36 tps (phi~0.228), with GHOST contributing ~0x. The proposal's projected 0.4-0.7 tps is NOT honest after the binding constraint: it assumed r_memo 0.30-0.45 at cos>=0.90, and the measured held-out cosine is 0.50, so the multiplier (1 - r_memo) stays at ~1.0.",
          "salvageable": [
            "The honest input-side measurements are correct and worth keeping: input vectors do not repeat (causal-prefix NN cos>=0.99 = 0.4%, cos>=0.90 = 3.1%), input cloud near-full-rank (rank50=19, rank90=134 of 256), bucket-hotness near-uniform — these cleanly KILL the naive VQ-the-input-vector idea and should be cited as a dead-end closure.",
            "The reframe that EXPERT IDENTITY recurs (E7 0.872) while the input vector does not is the correct object-level insight — but it points to U3 caching/substitution (resident-neighbor reuse), NOT to output surrogation. Fold the energy back into U3 eps and the C (resident cache) lever.",
            "The self-warming/backfill mechanism is legal under no-retrain, but empirically empty here; do not resurrect for output reuse. It could in principle warm a per-expert SUBSTITUTION neighbor table (function-space dedup, E15) rather than a surrogate — that is the only adjacent live thread, and E15's own prior (<10% siblings, router trained to avoid near-duplicates) suggests it is also weak.",
            "Redirect effort to the genuinely free levers the ledger names and GHOST does not improve: C (resident cache capacity, phi=1-h(C), h jumps 0.595->0.857->0.997 from cap 128->256->384), B (batch/MTP union amortization), and U3 eps substitution. These move phi; GHOST does not."
          ],
          "confidence": "High. The kill is a direct measurement on the exact data and forward harness the proposal specified as decisive, not a re-derivation. Three independent signatures converge: (1) held-out cosine 0.46-0.52 vs the proposal's own 0.90 kill bar; (2) TRAIN 1.000 / TEST 0.076 at full rank = unambiguous memorization-not-function signature; (3) per-context invocation budget of 4-7 samples/expert cannot fit a 229k-param surrogate. The only residual uncertainty is whether hand-picking the very most-reused experts and pooling across MANY contexts could lift cosine — but that violates the single-context / no-retrain framing and the near-full-rank input cloud (B5 corroborated) caps the achievable linear capture regardless."
        }
      }
    ],
    "beats_floor": []
  }
}