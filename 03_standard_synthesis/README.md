# 03 — Conceptless Std-Only Invention Chain

Gen0/1/2 engines built on the Zig stdlib alone (no VSA, no hypervectors) — the
first to produce measurable invention without semantic scaffolding, and the
lineage that led directly to DomainSpec (thread 04). See `docs/` and
../RESEARCH_INDEX.md.

```sh
zig build      # 6 executables
```
Depends on `../core`.
